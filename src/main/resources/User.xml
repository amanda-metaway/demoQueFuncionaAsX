<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="User">

    <select id="getUserById" parameterClass="integer" resultClass="com.example.demo.Model.User">
        SELECT id, name, perfil, cpfusuario, password
        FROM users
        WHERE id = #id#
    </select>


    <insert id="saveUser" parameterClass="com.example.demo.Model.User">
        <selectKey keyProperty="id">
            SELECT nextval('users_id_seq'::regclass)
        </selectKey>
        INSERT INTO users (id, name, perfil, cpfusuario, password)
        VALUES (#id#, #name#, #perfil#, #cpfUsuario#, #password#)
    </insert>


    <update id="updateUser" parameterClass="com.example.demo.Model.User">
        UPDATE users
        SET name       = #name#,
            perfil  = #perfil#,
            cpfusuario = #cpfUsuario#,
            password   = #password#
        WHERE id = #id#
    </update>

    <delete id="deleteUser" parameterClass="java.lang.Integer">
        DELETE
        FROM users
        WHERE id = #id#
    </delete>

    <select id="listarUsers" resultClass="com.example.demo.Model.User">
        SELECT id, name, perfil, cpfusuario, password
        FROM users ORDER BY id DESC
    </select>




</sqlMap>
